# ぷるりんレーサーズ - ゲーム仕様設計書

## 📖 プロジェクト概要

### 🎯 ゲームコンセプト

かわいい架空生物が、以下の要素を駆使して競うレースゲームです。

- ✅ **育成**: 属性 / ステータス 
- ✅ **戦略**: レース位置取り / ドラフティング / スタミナ管理
- ✅ **条件適応**: どの大会のレースに合わせるか / いつ仕掛けるか / 集団で力を温存するか

### 💡 基本方針

競馬ゲームのように、コースに沿って自動的に前進しながら、以下の要素を楽しみます。

- 📍 **左右移動**: 内側／外側へ位置を調整
- 🌪️ **カーブライン**: カーブでのライン取り
- ⚖️ **速度と旋回の関係**: スピードが高くなるほど制動が難しくなる

**制御方式**:
完全自由移動ではなく、以下の 2 つのパラメータで管理します。

```
distance:    コース開始地点からの進行距離（メイン軸）
offset:      コース中心からの左右位置（単位: m）
```

### 🧭 レース設計思想（重要）

このゲームでは「最適解」を作らない。

状況によって、

- 内側を選ぶ
- 外側を選ぶ
- 集団に残る
- 先頭へ出る

という判断が変化することを目標とする。

**設計理念:**

- コースによって有利な戦術が変わる
- 属性によって得意な状況が変わる
- 位置取りに意味を持たせる
- 集団戦術を成立させる
- 後方一気の戦略も成立させる
- 「最内を走り続けること」が常に正解にならないようにする

※ **風システム（向かい風・追い風・横風）は採用しない。**  
環境要因でレース展開を左右しすぎず、位置取り・集団・コース形状で勝負が決まるようにする。

※ 勝敗を決めるのは **能力配分・属性・脚質・トレーナー判断・レース中判断（ブースト含む）・位置取り**。ガチャ・個体値・ランダム成長、および従順度／トレーナースキル／mistake 系の揺らぎは採用しない（詳細は「トレーナーと NPC AI」）。

---



## 🏁 レースシステム



### 📊 ステータス・育成システム

限られたポイントを自由配分する育成システム。

ぷるりんの能力は、**プレイヤーが能力ポイントを配分することで決定**する。
能力値による個体差を明確にし、「速度特化型」「スタミナ型」「コーナー型」など、**極端な個性を作れる**ようにする。

#### 📋 基本ステータス一覧


| ステータス        | 役割                  |
| ------------ | ------------------- |
| **最高速**      | 到達できる最大速度           |
| **スタートダッシュ** | スタート、速度上昇速度、立ち上がり性能 |
| **巡航力**      | 中盤で速度を維持する能力        |
| **スタミナ**     | 長距離性能、速度維持能力、残スタミナ量 |
| **ラストスパート**  | 終盤スパート時の加速性能        |
| **加速力**      | 瞬間的な加速力、負荷をかけた走行能力  |
| **心肺性能**     | 高負荷状態への耐性、心拍上限      |
| **空力適性**     | 空気抵抗への耐性（単独走向き）     |
| **集団適性**     | ドラフティング効果（集団走行向き）   |
| **接触耐性** | 他キャラや柵との接触時に耐える力 |
| **操作性**      | コーナリング性能、ライン維持能力    |




#### 💪 育成例

- **速度特化型**: 最高速 / スタートダッシュ / ラストスパート → 短距離・直線勝負に強い
- **スタミナ型**: スタミナ / 心肺性能 / 集団適性 → 長距離・持久戦向き
- **コーナー型**: 操作性 / 加速力 / 巡航力 → テクニカルコース向き



#### ❌ 採用しない育成要素

勝敗を「運」ではなく「判断」に寄せるため、以下は採用しない。

- 育成ガチャ
- 個体値
- ランダム成長

能力は **ポイント配分（＋属性ボーナス）で決まる固定値** とする。同じ配分なら、同じ条件では同じ能力として扱う。

### 🎒 所持スロットとレシピ

楽しさの中心は **ポイント配分の取捨選択** にある。所持枠の解放やガチャで枠を増やす進行は設けない。

#### 所持スロット（レースに出せる個体）

| 項目 | 内容 |
|------|------|
| **上限** | **最大 5 体** |
| **解放** | **なし**（最初から最大 5 まで作成・所持できる） |
| **不採用** | ガチャ、ストーリー進行による枠解放、条件付きスロット解放 |

レースへ出走させる個体は、この所持スロットから選ぶ。

#### レシピ（配分テンプレート）

所持とは別に、**レシピ**（仮称）として次の組み合わせを任意に保存できる。

- 能力ポイントの配分
- 脚質
- 属性

| 項目 | 内容 |
|------|------|
| **件数** | ユーザーが望めば多数保存できる（実質困らない件数。厳密な無制限保証は実装時にソフト上限などで調整） |
| **重複** | 配分ポイントの総量は全員同一のため、**同一内容のレシピが複数あってもよい** |
| **用途** | 実験・お気に入り戦術の保管。所持個体への適用・差し替え |

所持＝「いまレースに出せる布陣」、レシピ＝「配分の実験庫／テンプレート」と役割を分ける。

#### 他プレイヤーのデータ

将来は、他プレイヤーのぷるりん／レシピ相当データを **取得・コピー** できるようにする。  
コピー後は自分用に改変してよい。総ポイントが同一なので、完コピ同士が並んでも公平性は壊さない。

※ β では **自アカウント内のレシピ保存・読込・所持への適用** を先行し、他者共有・コピーの本番仕様は後続とする。

---



### 🏃 レース中の状態値

能力値とは別に、レース中にリアルタイムで変化する値を持つ。


| 状態値 | 内容 |
| ------ | ---- |
| **現在速度** | 現在の走行速度 |
| **目標スピード** | プレイヤー（または CPU）が指示するペース。現在速度はこれに向かって追従する |
| **残スタミナ** | 使用可能な体力 |
| **心拍数** | 現在の身体負荷 |
| **モチベーション** | 精神状態、能力発揮率 |
| **出力** | 目標への追従に要する内部負荷（導出値。プレイヤーの主操作にはしない） |
| **集団位置** | 先頭 / 集団 / 中団 / 後方 |
| **ブースト状態** | 発動中か、冷却状態か |


### 🎯 目標スピードと加速

プレイヤーは **現在速度を直接操作しない**。代わりに **目標スピード** を指示する。

```
目標スピードを設定（粗い刻みの増減）
↓
現在速度が目標へ近づく
（加速力が大きいほど／空気抵抗が少ないほど上がりやすい）
↓
天井は最高速（ブースト中の上限扱いは実装時に一文で固定）
```

- 空気抵抗は単独走行時に大きく、**ドラフティング**で軽減される（空力適性・集団適性も効く）
- 目標を **急に大きく上げる**（現在との乖離が大きい／目標の変化が大きい）ほど、心拍負荷が上がりやすい
- 刻み幅はテストしながら決める。**帯プリセット（巡航／アタック等のボタン併用）は採用しない**（特にスマホで操作が煩雑になりやすいため）

**出力**は「目標に追いつくためにどれだけ負荷がかかっているか」の内部値とし、心拍・スタミナ消費の説明に使う。HUD 表示は必須ではない。

---



### ❤️ 心拍システム

**疲労システムは採用しない。** 代わりに、**心拍数 ＋ 残スタミナ** で身体負荷を表現する。

**基本動作:**

```
目標スピードを上げる／現在速度との乖離が大きい
↓
出力（追従負荷）が増える
↓
心拍数上昇
↓
残スタミナ消費増加
↓
高心拍状態が続くと能力低下
```

**負荷の例:**


| 走行状態 | 心拍 | 残スタミナ |
| ------ | ---- | -------- |
| 通常巡航（目標と現在が近い） | 130 前後 | 少量消費 |
| 高ペース追従・急加速狙い | 170 前後 | 大幅消費 |
| 限界走行 | 心拍上限付近 | オーバーヒート状態へ |




#### 🫀 心拍上限（心肺性能）

心肺性能によって最大心拍数が変化する。


| タイプ      | 特徴            |
| -------- | ------------- |
| **低心肺型** | 短時間なら強いが限界が早い |
| **標準型**  | 平均的           |
| **高心肺型** | 高負荷状態を長時間維持可能 |


**例:** 通常個体は最大心拍 180、心肺特化個体は最大心拍 200。

---



### 🔥 ブーストシステム（鞭要素）

競馬の鞭、自転車競技のアタックに相当する仕掛け要素。

#### 基本仕様

- **1 レース最大 3 回**
- **1 回 3 秒**
- 発動中は加速度上昇、および目標スピード／速度上限の一時的な引き上げ（詳細は実装時に一文で固定）
- 心拍数が急上昇し、残スタミナを大量消費



#### 連続ブースト

ブースト中に再使用可能。


|           | 内容                          |
| --------- | --------------------------- |
| **メリット**  | 加速度アップ → 一気に前へ出る            |
| **デメリット** | 心拍限界突破 → オーバーヒート時間増加 → 後半失速 |




#### 戦略例


| 仕掛け方         | メリット   | デメリット      |
| ------------ | ------ | ---------- |
| **早仕掛け**     | 位置を取れる | ゴール前失速のリスク |
| **最終直線ブースト** | 最大効果   | 前が詰まる可能性   |


---



### 🔮 属性システム（属性ボーナス）

基本属性：地・水・火・風

属性は **初期能力補正と能力上限補正** を持つ。

#### 📋 属性と補正の方向性


| 属性    | 補正の方向性                  |
| ----- | ----------------------- |
| **地** | スタミナ、接触耐性、安定性 |
| **水** | 操作性、集団適性、柔軟性            |
| **火** | スタートダッシュ、ラストスパート、ブースト性能 |
| **風** | 最高速、空力適性、巡航力            |


**例（風属性 = 高速型）:**

```
通常個体: 最高速 最大 10
風属性:   初期 +3 / 上限 +5 → 最高速 最大 15
          体重軽減
```

※ 数値はプロトタイプ用の仮スケール。  
※ 属性とコース適性の連携は「コース移動方式」章を参照。  
※ 属性名の「風」は元素属性であり、気象の風システム（不採用）とは無関係。

---



### 🏇 脚質システム

脚質は**レース方針**であり、能力値ではない。


| 脚質     | 特徴        |
| ------ | --------- |
| **逃げ** | 先頭維持、序盤強い |
| **先行** | 前方集団で戦う   |
| **差し** | 集団内で温存    |
| **追込** | 後方待機、終盤勝負 |




#### 脚質ボーナス（状況適性）

能力補正ではなく、**脚質に合った状況での適性ボーナス**とする。


| 脚質     | 状況     | 効果の例                   |
| ------ | ------ | ---------------------- |
| **逃げ** | 先頭にいる  | モチベーション +10%／心拍上昇は少し増加 |
| **差し** | 集団内にいる | 残スタミナ消費減少              |
| **追込** | 終盤     | ブースト効率上昇               |


（先行は「前方集団での安定」など、実装時に調整）

---



### 😊 モチベーションシステム

精神状態を表し、**能力発揮率**に影響する。
乱数ではなく、**脚質と現在位置から決まる決定的な補正**。

- 脚質に合った位置にいる → 上昇／維持
  - 逃げが先頭 → モチベーション上昇
  - 追込が後方集団 → モチベーション維持
- 脚質に合わない位置 → 低下
  - 逃げが後方待機 → モチベーション低下

---



### 💨 ドラフティング（空気抵抗軽減）システム

前方の生物の後ろにつくことで空気抵抗を減らす。自転車競技のように機能する。

**計算基準:** 前方生物との位置関係のみ。気象・風向きは使わない。

#### 📊 空気抵抗率（例）


| ポジション    | 空気抵抗率    |
| -------- | -------- |
| **先頭**   | 100%（基本） |
| **2 番手** | 80%      |
| **集団中央** | 60～70%   |
| **最後尾**  | 50～60%   |




#### ✅ ドラフティングの効果

- スタミナ消費減少
- 速度維持
- 終盤のスパート用にスタミナを温存



#### 🏃‍♂️ 先頭走行のメリット例

- わずかな速度上昇
- 観客による気力上昇
- 逃げ成功ボーナス

---



## 🛣️ コース移動方式



### 📍 位置管理の基本（distance + offset）

```yaml
distance:
  unit: meter
  meaning: コース開始地点からの進行距離

offset:
  unit: meter
  min: -6.0   # 内側
  max: +6.0   # 外側
```

- **負の offset**: 内側
- **正の offset**: 外側
- 実際の可動範囲はコース幅に合わせてクランプする

**位置決定の流れ:**

```
distance = 500m
↓
Curve3D から 500m 地点の座標を取得
↓
その地点の中心位置 + offset（左右）
↓
キャラクター位置決定
```

---



### 📐 生物サイズとコース幅（最大 12 頭対応）

**コース幅・ゲート素材は最大 12 頭まで対応できるサイズで最初から揃える。**  
実際の出走数（基本は 8 頭）とは切り離して考える。

```yaml
creature_width: 0.8    # m
minimum_gap: 0.3       # m
# 1 体あたり必要幅 ≈ 1.1m
# 12 体横並び ≈ 13.2m 以上
```

| 項目         | 値   |
| ---------- | --- |
| **最小コース幅** | 14m |
| **推奨コース幅** | 16m |
| **最大コース幅** | 20m |

※ コースタイプの「広い／狭い」は、絶対幅ではなくカーブの鋭さ・直線比率の相対表現とする。  
※ 基本出走が 8 頭でも、幅は 12 頭分を確保する。集団密度は AI の位置取り（理想ラインへの寄り）などで調整する。

#### 🚪 スタートゲート

- **ゲート枠は最初から 12 頭分の素材・配置を用意する**
- 8 頭レースでは、そのうち使う枠だけ出走させる（未使用枠は空／閉など演出は実装時に決める）
- 使う・使わないはレース設定次第。作り直しを避けるため、ゲート資産は 12 頭分で固定

---

### 🏇 出走数の方針

| 項目 | 内容 |
|------|------|
| **基本出走数** | **8 頭** |
| **エンジン上限** | **最大 12 頭**まで対応できる設計 |
| **β 版** | **8 頭固定**でバランス・AI・観戦しやすさを優先調整 |
| **人数不足時** | 受付締切時点の空き枠を **CPU（NPC）で補充**し、常に 8 頭にする |
| **コース幅・ゲート** | 最初から **12 頭分**を用意（上記） |

**なぜ基本は 8 頭か（要約）:**

- 脚質（逃げ・先行・差し・追込）の位置取りが分かりやすい
- 「誰の後ろに付くか」の判断が薄れにくい
- 観戦・配信で順位と動きが見えやすい
- AI・接触・ドラフト判定の負荷が抑えられる（サーバー向き）

**8 頭時の脚質配分の目安例:**

| 脚質 | 頭数目安 |
|------|----------|
| 逃げ | 1 |
| 先行 | 2 |
| 差し | 3 |
| 追込 | 2 |

（実際のエントリーで偏ってもよい。目安）

**将来の出し分け例（必須ではない）:**

| レース種別 | 出走数の例 |
|------------|------------|
| 通常レース | 8 頭 |
| 大型大会（GⅠ 相当） | 10 頭 |
| 特別イベント | 12 頭 |

面白さが確認できてから、10 頭・12 頭のテストレースを行う。

---



### 🎯 Godot の Path3D / Curve3D 採用理由

- ✅ 自然なコース形状に対応（円形・三角形・自由曲線など）
- ✅ オンライン同期が容易
- ✅ コース上の距離を進めるだけで位置決定できる

**実装方式:**

1. コース上の距離を進める
2. その地点の座標を取得
3. offset 分だけ左右へずらす

---



### 📏 カーブ率と内外ライン差（距離補正）

「ここはカーブ」と手動設定する方法もあるが、
**コース形状から自動計算する方式**を採用する。

- 🔄 コース形状に応じた自然な曲がり方
- 📐 円形・三角形・自由曲線などに対応可能
- 💻 オンライン同期が簡易的（距離を共有すれば OK）



#### 🌊 カーブ率（curve_strength）の計算

前方方向ベクトルと次方向ベクトルの角度差から求める。

```
curve_angle = angle(previous_direction, next_direction)

curve_strength = clamp(curve_angle / 90.0, 0.0, 1.0)
```


| curve_strength | 内容     |
| -------------- | ------ |
| 0.0            | 完全な直線  |
| 0.3            | ゆるいカーブ |
| 0.7            | 急カーブ   |
| 1.0            | ヘアピン   |




#### 📐 内外ライン差：距離倍率方式（distance_multiplier）

内外補正は **実効速度倍率ではなく、必要走行距離倍率** とする。

```yaml
line_bonus_type: distance_multiplier
```

意味：同じ対地速度でも、内側は短い中心線距離で済み、外側は余分に進む必要がある（競馬的で自然）。

**直線:** 曲率が無いので倍率は常に **100%**（内外で差を付けない）。

**カーブ（M2 実装・2026/08/26）:**

- 中心線の曲率 κ（≈1/R）から、幾何比 `(R + offset) / R` ＝ `1 + offset × κ` を基本にする  
  （見た目の流れる速さを内外で揃えやすい）
- 感触調整として **`GEOMETRIC_BLEND`（現状 0.85）** で 1.0 との間を取る  
  （完全な半径どおりより、内側有利・外側不利をやや抑える）
- コード正本: `client/scripts/m2_track_math.gd`（定数 `GEOMETRIC_BLEND`）

参考（コース中心半径 18m・可動端 offset ±6.75m・BLEND 0.85 のとき、端でおおよそ内側 **約 68%**／外側 **約 132%**）。数値は感触で変えてよい。

**弱い差の調整例（将来・curve_load と合わせて再検討してよい）:**


| ライン    | 距離倍率 |
| ------ | ---- |
| **内側** | 97%  |
| **中央** | 100% |
| **外側** | 103% |


カーブが強いほど差を広げてもよい（調整用の例）:


| カーブタイプ | 内側   | 中央   | 外側   |
| ------ | ---- | ---- | ---- |
| 直線     | 100% | 100% | 100% |
| 普通カーブ  | 97%  | 100% | 103% |
| 急カーブ   | 94%  | 100% | 106% |


※ 以前の表（内側 103% / 外側 97%）は距離倍率としては逆であり、採用しない。  
※ M2 は上表の弱い ±% ではなく、幾何＋BLEND を採用している（見た目速度優先のたたき）。

**現実の距離差の参考例:** 180 度カーブ、中心半径 20m・コース半幅 8m 程度なら、内外で走行距離に差が出る。ゲームでは現実値をそのまま使わず、上記のとおり調整する（内側が強すぎる／弱すぎるのを防ぐ）。

#### 🚦 カーブ負荷（curve_load）

内側が常に有利になる問題を防ぐため、
**「速度が高いほどカーブが難しくなる仕組み」**を入れる。

**初期実装（採用）:**

```
curve_load = speed × curve_strength
```

`speed² × curve_strength` は急激すぎる可能性があるため、初期は線形を採用する。

**将来の候補（必要なら）:**

```
curve_load = pow(speed, 1.5) × curve_strength
```

- **低速**: 問題なく曲がれる
- **高速**: 外側へ膨らむ、減速が必要



#### 🏃‍♂️ 旋回性能（コーナリング能力）

各生物に「旋回性能」または「コーナリング性能」を持たせる。

**判定例:**

```
必要旋回能力 > 生物の旋回性能の場合:
  ・外側へ膨らむ
  ・速度低下
  ・スタミナ消費増加
```



#### 📍 ライン選択の駆け引き

「内側が常に正解」ではなく、状況によってラインを選ぶ。


| ライン    | メリット              | デメリット                |
| ------ | ----------------- | -------------------- |
| **内側** | 走行距離が短い           | 曲がる難易度が高い / 前が詰まりやすい |
| **中央** | 安定                | 特になし                 |
| **外側** | 曲がりやすい / 追い抜きしやすい | 走行距離が長い              |


**状況に応じたライン選択例**


| 状況      | ライン  | 理由           |
| ------- | ---- | ------------ |
| **先頭**  | 内側   | 距離有利         |
| **後方**  | 外側   | 加速して追い抜く     |
| **集団中** | 中央付近 | ドラフティングで空力維持 |




#### 🔥 属性とコース適性の連携


| 属性    | 特徴                      | コース適性         |
| ----- | ----------------------- | ------------- |
| **地** | ・重心が低い ・カーブ安定 ・悪路に強い    | テクニカルコース / 山岳 |
| **水** | ・柔軟 ・旋回性能が高い ・内側ライン向き   | 雨 / コーナー重視コース |
| **火** | ・加速性能が高い ・直線向き ・急カーブは苦手 | 短距離 / 直線勝負    |
| **風** | ・最高速度が高い ・軽量 ・高速走行向き    | 高速コース / 長距離   |


---

## 🧱 接触・柵・コース形状の判定方針

オンライン・最大 12 頭（基本 8 頭）・`distance + offset` 方式のため、**Godot の物理 Collision をそのままゲームルールに使わない**。

### 大方針

| 対象 | ルール上の扱い | Godot 物理 Collision |
|------|----------------|----------------------|
| **他キャラ同士** | `distance` / `offset` の自前距離判定 | **使わない**（押し合い・反発しない） |
| **柵** | `offset` が左右限界に達した＝接触 | **ルールには使わない**（見た目用のみ可） |
| **地面** | 位置は Path 上の点＋offset で決定 | プロトタイプでは **不要寄り** |
| **カーブ／上り坂** | いまの `distance` から Curve3D を読んで計算 | 手動ゾーン配置は本判定に使わない |

キャラの最終位置は、物理結果を信じず **Path 上の点（distance）＋左右（offset）で上書き**する。

### 他キャラとの接触

横・縦の距離で判定する。

```
横: abs(offsetA - offsetB)
縦: abs(distanceA - distanceB)
```

例: 横 0.9m 以内かつ縦 0.8m 以内なら「接触中」。

接触後は完全停止ではなく、**目標 offset を少しずらして避ける**（例: 0m を目指していたが前に人がいれば +0.6m へ）。押し合いの強さは **接触耐性** などでゲームルールとして表現する。

#### 占有レーン（Reservation）

各ぷるりんが目指す `target_offset` が他キャラに占有されていれば、空いている隣へ少しずつ妥協する。

```
例: -2.5m が埋まっている → -1.8m → -1.2m
```

物理で押し合わなくても、集団走行・進路変更・ドラフティングと相性が良く、サーバーでも同じロジックを再現しやすい。

### 柵（コリジョンなしを基本）

**ルール上の柵は Collision ではない。**

```
offset が ±上限に達した
　＝ 柵に接触中
　＝ キャラ同士の接触と同系統の処理
```

| 処理 | 内容 |
|------|------|
| めり込み防止 | `offset` を上限でクランプ |
| ペナルティ例 | 減速、モチベーション低下など |
| 接触耐性 | 柵接触のキツさにも使える（任意） |

キャラ同士は相手が動くため押し合い・回避が必要だが、柵は動かない壁なので **クランプ＋ペナルティ** で足りる（完全同一コードである必要はない）。

#### 見た目の柵の置き方

手で 1000 個置く必要はない。

| 段階 | やり方 |
|------|--------|
| **プロトタイプ** | 柵メッシュなし。`offset` の上下限だけ |
| **見た目が必要** | **Path（Curve3D）に沿って左右へ自動生成** |
| **手置き** | ゲート・看板などスポットのみ |

Path からの生成イメージ:

```
distance を少しずつ進む
↓
その地点の左右へ ±コース半幅 ずらす
↓
柵メッシュを置く（または MultiMesh にまとめる）
```

見た目は柵モデルでよく、将来当たり判定を足す場合も **長い薄い箱（見えない壁）** を Path に沿って少数置く程度で足りる。見た目の柵メッシュそのもので細かい Collision を大量に持たない。

### 地面・高低差（坂）

高低差をまだ入れない段階では、**地面との物理接地は必須ではない**。

将来、中山競馬場のような坂を入れる場合も、地面物理ではなく **Curve3D 自体に高低差を入れる**方式を取る。

```
上り: 出力・心拍・スタミナ消費が増える等（任意）
下り: 巡航しやすい等（任意）
```

これは「地面に乗っている物理」ではなく、**コース形状パラメータとしての坂**。

### カーブ期間・上り坂の判定場所

手動で「ここからカーブ」「ここから上り」ゾーンを並べる方式は、本判定には使わない（Path とズレやすい／メンテが増える）。

**いまの `distance` から Curve3D を読んで都度計算する。**

| 知りたいこと | 出し方 |
|--------------|--------|
| カーブか／強さ | 前後の進行方向の角度差 → 既存の `curve_strength` |
| 上り／下り | 少し先の点と今の点の高さ差 → 勾配 |
| 直線か | `curve_strength` が十分小さい |

サーバー／クライアント／リプレイで、同じ Curve データと同じ `distance` があれば同じ結果になる。

手動ゾーンが向くのは、スタート・ゴール・特殊イベントなど **形から自動では分からない意味**がある地点だけ。

### 初期プロトタイプの構成まとめ

| 対象 | 実装方法 |
|------|----------|
| コース移動 | Path3D + `distance` / `offset` |
| 地面 | 物理接地なしで可（位置は Path で決定） |
| 柵（ルール） | `offset` クランプ＝接触。Collision なし |
| 柵（見た目） | 後回し可。必要なら Path 左右に自動生成 |
| 他キャラ | 物理 Collision なし。`distance` / `offset` 判定 |
| ドラフティング | `distance` / `offset` のみ |
| 接触・押し合い | 自前ロジック（接触耐性・位置関係） |
| カーブ／坂 | `distance` 地点の Curve 情報から計算 |

---

## 🏁 レース戦略・操作方式



### 📚 基本的なレース要素

- **能力**: 基本ステータス（最高速〜操作性）＋属性ボーナス
- **状態**: 残スタミナ / 心拍数 / モチベーション / ブースト状態
- **戦術**: 脚質 / 位置取り / ドラフティング / ブーストのタイミング
- **コース要因**: コース形状（直線・カーブ・ヘアピン）、距離カテゴリー

※ 気象風は扱わない。  
※ 脚質（逃げ・先行・差し・追込）の定義は「脚質システム」を参照。

### 🎮 操作方式（プレイヤー）

プレイヤーが直接操作する主操作は次のとおり。**現在速度は直接操作しない**（目標スピードを指示し、ぷるりんが追従する）。


| 操作 | 内容 |
| ---- | ---- |
| **左右移動** | 内外のライン取り（offset の指示） |
| **目標スピード** | 上下などで粗い刻み増減。自分でペースを作る |
| **ブースト発動** | 1 レース最大 3 回の仕掛け |


- **積極／温存などのペース帯プリセットはプレイヤー操作には使わない**（目標スピード操作に任せる）
- 内へ／外へ／スパート準備など、ペース以外の戦術指示を残すかは実装・UI を見ながら整理してよい

競馬ゲームの騎手・ロードレースの監督に近い立ち位置。

```
プレイヤー: 状況判断 → 目標スピード・左右・ブースト
ぷるりん:   能力・脚質・状態 → 現在速度の追従と走行
```

### 📷 カメラ・視点・ミニマップ

レース中の情報把握は、**カメラ切替**と**ミニマップ**で行う。  
プレイ中・リプレイ・観戦で **同じカメラ／切替ロジック** を使い、いつでも切り替え可能とする。

#### カメラアングル

| アングル | 内容 | 備考 |
| ------ | ---- | ---- |
| **後方** | 対象ぷるりんの後ろから追従 | **デフォルト** |
| **前方** | 対象の前から見る | |
| **横** | 対象の横から見る | |
| **頭上やや後方** | 斜め上から俯瞰気味に追従 | 本番候補 |
| **真上俯瞰** | 真上から見下ろす（正射影可） | **M2 たたき**で速度感確認用。C キー切替 |

※ M2 試作（`camera_m2.gd`）は **後方追従** と **真上＋正射影** の2種。斜め俯瞰は遠近で速度が分かりにくかったため採用していない。本番の「頭上やや後方」は後で差し替えてよい。

#### カメラ対象

- デフォルトの対象は **自分の操作ぷるりん**
- **他のぷるりん**にもカメラを付け替え可能（自分専用視点に固定しない）
- 対象の切替は、後述のミニマップ／前後スライド選択と連携する

#### カーブ時のカメラ挙動（保留）

カーブでカメラを外側へ振るかどうかは **未確定**。

- 開発が進んでから実機テストで判断する
- または設定画面で ON/OFF できるオプションにする

初期実装では **ほぼ真後ろ固定** で進め、挙動は後から差し替えやすい形にしておく。

#### ミニマップ・個体選択

後方カメラだけでは見えにくい「後方の相手」や「集団全体」は、ミニマップと UI で補う。


| 要素 | 内容 |
| ---- | ---- |
| **ミニマップ** | 集団全体の位置関係を表示。倍率変更・前後スクロールが可能 |
| **前後スライド選択** | 特定キーで前後のぷるりんを順に選択 |
| **選択時の利用** | 選択中ぷるりんのステータス情報表示、およびその個体へのカメラ切替 |


※ キー割り当て・UI 配置の詳細は実装時に決める。方針として「集団把握＝ミニマップ」「個体注目＝スライド選択＋カメラ」とする。

---



## 🧠 トレーナーと NPC AI



### 🎯 位置づけ

ジャンル感としては、

**競馬ゲーム ＋ 自転車ロードレース戦術** の中間。

マリオカート型のアイテム・直接ドリフト操作レースではない。

プレイヤーは **トレーナー**（騎手兼調教師に近い役割）として作戦と指示を出す。  
実際に走るのは **ぷるりん**。

```
プレイヤー（トレーナー）
    ↓ 作戦・指示
ぷるりん（能力・脚質に基づき走行）
```

同じ能力のぷるりんでも、トレーナーの作戦・指示が違えば走りが変わる。  
負けたときに「運が悪かった」ではなく、「作戦ミス／配分ミス」と感じられることを目指す。

### 👤 トレーナーの画面表現

レースの視覚的主役は **ぷるりん**。公式の人型トレーナー（3D・立ち絵をゲーム内必須アセットとする扱い）は置かない。

| 区分 | 方針 |
|------|------|
| **キービジュアルの人物** | 宣伝イラストとして切り分ける。プレイ必須アセットにはしない |
| **トレーナーの識別** | ユーザー名（CPU はトレーナー名）、任意イラスト（ユーザーが用意可）、あいさつ文、戦績などの **プロフィール表示** |
| **表示場所の例** | ロビー、結果、対戦カードなど。レース中メイン視点への常時大表示はしない |
| **観客席の人間** | 背景・演出として観戦者がいる程度は **可**（長編ストーリーや会話イベントの前提にはしない） |
| **トラックサイドのトレーナー配置** | 採用しない（旧検討の案 C） |
| **会話・長編ストーリー** | 現時点では設けない |
| **チュートリアル等** | **オフライン**で提供。説明用立ち絵は将来可（必須ではない） |
| **導入用コンテンツ** | 短い **トライアル／クエスト形式**（操作・配分の習熟）。オフライン。本数・UI は実装時に詰める |
| **本格練習・本番レース** | **オンライン必須**（非ランク練習枠の詳細は今後） |

※ ユーザー画像の本番アップロード（審査・制限）は実装時に別途決める。β は枠＋デフォルト／仮置きでよい。

### 📶 習熟コンテンツと回線

| 区分 | 回線 | 備考 |
|------|------|------|
| チュートリアル | オフライン | クライアント完結。本番判定との完全一致は必須にしない |
| 導入トライアル／クエスト | オフライン | 短い課題形式。長編ストーリー・会話イベントにはしない |
| 本格練習（8 頭・本番相当ルール） | オンライン | 非ランク等は今後。サーバー判定を再利用 |
| 本番マッチ | オンライン | 開催サイクル＋必要時 CPU 補充 |

### ❌ この章で採用しないもの

余計な運要素とみなして、**採用しない**。


| 非採用                             | 理由                 |
| ------------------------------- | ------------------ |
| ぷるりんの従順度                        | 指示無視が乱数化しやすい       |
| トレーナースキル（補正付きパッシブ等）             | 収集・ガチャ寄りになりやすい     |
| mistake / reaction_error などの揺らぎ | 完全再現防止のための「擬似運」になる |


NPC に「最適ライン・最適加速を知る正解 AI」も与えない。  
判断は脚質・性格パラメータ・状況ルールに基づくが、**最適解を計算して常に正解を取る AI にはしない**。

### 🧩 役割分離

```python
class Pururin:
    # 能力（ポイント配分の結果 + 属性ボーナス）
    top_speed, start_dash, cruise, stamina, last_spurt
    accel, cardio, aero, pack, contact_resist, handling
    attribute  # 地水火風
    running_style  # 逃げ / 先行 / 差し / 追込

class Trainer:
    # 固定の作戦傾向（事前設定。育成ガチャではない）
    aggression      # 前目志向
    patience        # 温存・我慢
    drafting_pref   # 集団・ドラフト志向
    corner_pref     # 内ラチ / ライン志向 など
```


| 担当 | 決めること |
| ---- | -------- |
| **Trainer（プレイヤー）** | 目標スピード、左右、ブースト、（任意の）戦術指示 |
| **Trainer（CPU）** | 目標順位帯、積極／温存などのペース方針、内外・スパート方針 → `target_speed` 等へ落とし込む |
| **Pururin** | 能力と脚質に従い、`target_offset` / `target_speed` へ実際に動く |




### 🏁 脚質（running_style）


| 脚質     | 目標順位の目安 | 傾向         |
| ------ | ------- | ---------- |
| **逃げ** | 1〜2 位   | 先頭を狙う      |
| **先行** | 3〜5 位   | 前方で機会をうかがう |
| **差し** | 6〜9 位   | 集団で温存し終盤   |
| **追込** | 後方寄り    | 終盤勝負       |


脚質は「理想の集団位置」の初期方針になる。レース中指示で上書きできる。

### 🎮 レース中の指示（プレイヤーと CPU）

完全なスティック操作レースではなく、**目標を出してぷるりんが解釈・実行する**。


| 種別 | プレイヤー | CPU（NPC） |
| ---- | -------- | -------- |
| **脚質プリセット** | 逃げ / 先行 / 差し / 追込（出走前） | 同左 |
| **ペース** | **目標スピードを上下で直接指示**（粗い刻み） | **積極／温存などの方針**を検討し、`target_speed` に写像してよい |
| **レース中のその他** | 左右、ブースト（戦術指示は要整理） | 内へ／外へ／スパート準備／ブースト判断など |




### 🏆 最終的な勝敗要素

勝敗は以下で決まる。

1. **能力配分**
2. **属性**
3. **脚質**
4. **トレーナー判断**（作戦・性格パラメータ）
5. **レース中判断**（指示）
6. **ブースト使用タイミング**
7. **位置取り**

運ではなく、**「いつ仕掛けるか」「どこで我慢するか」「誰の後ろにつくか」**が重要になる。

### 🤖 NPC AI の基本方式

NPC（他トレーナー）も、キャラを直接スティック操作するのではなく、

```
目標を決める → そこへ向かう
```

方式とする。

**トレーナー AI（方針決定）の優先度例:**

```
priority 1: 勝負位置へ移動
priority 2: スタミナ温存
priority 3: ブースト判断
```

**ぷるりん AI（実行）:**

```
現在状態を判断 → 指示を解釈 → 走行
```



#### 3 層構造

```
戦略 AI（数秒〜1 秒単位）
    ↓
行動 AI（おおよそ 0.2 秒）
    ↓
操舵 AI（毎フレーム）
```


| 層         | 更新頻度の目安    | 内容                              |
| --------- | ---------- | ------------------------------- |
| **戦略 AI** | 約 1 秒に 1 回 | 先頭へ行くか／集団に残るか／スタミナ温存か／追い込み開始か   |
| **行動 AI** | 約 0.2 秒    | 内へ／外へ／加速／減速などの具体行動              |
| **操舵 AI** | 毎フレーム      | `offset` / `target_speed` へ近づける |


**処理の流れ:**

```
npc.update()
  → decide_strategy()
  → decide_target_position()
  → decide_target_offset()
  → speed_control()
```

**戦略 AI の例（概念）:**

- スタミナが低い・心拍が高い → 温存
- 終盤 → 攻撃（仕掛け）・ブースト判断
- 空気抵抗が大きい状況 → ドラフト位置を優先

**行動 AI の例（概念）:**

- 前が詰まった → 外へ出す
- 急カーブ → 速度を落とす

**ライン取りの例（属性・方針との連携）:**

- 水寄り・内重視 → カーブ前に内へ
- 火寄り・直線重視 → 外へ出して加速

（実装時は属性・`corner_pref`・指示の組み合わせで決める）

**スタミナ管理（必須寄り）:**

現在スタミナ・残り距離・順位を見て、

- まだ遠い → 温存
- 残りわずか（例: 300m）→ 全力



### 🪜 初期実装のおすすめ範囲

まずは次だけで「生き物がレースしている感」を出す。

- 脚質
- トレーナー性格パラメータ（aggression 等）
- 簡単な位置取り（目標順位 → offset / speed）

トレーナースキルツリー、従順度、意図的ミス率は入れない。

---



## 🎨 ゲーム体験デザイン



### 🦄 キャラクターデザイン



#### 🎨 イメージ

- スライム系 / 変形生物系 / まるいフォルム
- シルエット / 動き / 特徴的なアクセント



#### 🔮 属性ごとのデザイン案


| 属性    | デザイン要素   |
| ----- | -------- |
| **水** | 透明感、泡、水流 |
| **地** | 岩、鉱石、苔   |
| **火** | 炎、光る核    |
| **風** | 羽、流線型    |




#### 🎬 モーション設計

リアルな動物より、架空生物らしい動きを重視。

- **スライム系**: 伸びる / 縮む / 跳ねる
- **パワー型**: 大きく踏み込む / 地面を揺らす
- **高速型**: 体を流線型に変形する

---

### 🎭 見た目カスタム（3D 共通ボディ ＋ 画像パーツ）

方針: **3D モデルは共通／差は画像で表現**する。かわいさ・カスタム性・軽量・将来の MOD と相性が良い。

※ 模様・ほっぺ・表情パーツは **見た目専用**。能力値や勝敗には影響しない（能力はポイント配分で決める方針と分離）。

#### 役割分担

| 種別 | 例 | 方式 |
|------|----|------|
| **3D（共通／後から拡張）** | Body、耳、しっぽ、羽、ツノ、アクセサリ | 立体感が必要なもの。初期は Body 共通でよい |
| **画像パーツ** | 目、口、眉、ほっぺ、体模様、ハイライト、汗・涙・怒りマークなど | 透明背景 PNG を差し替え |

#### 表情パーツ（画像）

目・口・眉などを **別 PNG** にし、組み合わせで表情を作る。

```
eyes/     normal.png, happy.png, sleepy.png, angry.png ...
mouth/    smile.png, omega.png, open.png, sad.png ...
eyebrows/ normal.png, angry.png, worried.png ...
cheek/    blush.png, heart.png ...   # 任意
```

**描画エリアはテンプレートで固定する**（左目・右目・口の範囲を決める）。AI 生成や MOD で位置・大きさがズレないようにする。

レース中は状態に応じて表情を切り替えられる（ブースト開始、スタミナ切れ、追い抜き、勝敗など）。

初期は種類を抑えめにする（例: 目 3 × 口 3 × 眉 2）。大量バリエーションやユーザー MOD は後続。

#### 模様・ほっぺなど（見た目専用）

| 要素 | 内容 |
|------|------|
| **ほっぺ** | 赤ほっぺ、ハート、星など。かわいさが出やすい |
| **模様** | 属性・個体の見た目差（水玉、岩模様、炎模様など）。同じ能力でも見た目の個性を出せる |
| **イベント用マーク** | 汗・キラキラ・怒りマークなど |

模様は「育てる／集める楽しさ」用。**能力ガチャにはしない**。

#### 実装の進め方（目安）

1. 公式テンプレ固定 ＋ 公式パーツ数種
2. レース中の表情切替
3. 模様・ほっぺ
4. ユーザー追加／配置 JSON／表情パック配布は β 以降で検討

将来の柔軟な配置例（必須ではない）:

```json
{
  "eyes":  { "texture": "happy.png", "x": 0, "y": -4, "scale": 1.0 },
  "mouth": { "texture": "smile.png", "x": 0, "y": 12, "scale": 0.9 }
}
```

---

### 🏷️ ぷるりんの命名ルール

プレイヤーが付ける名前（愛称）の制限。後から厳しくすると不満が出やすいため、初期から固定する。

| 項目 | 内容 |
|------|------|
| **文字数** | 2〜12 文字 |
| **使用できる文字** | ひらがな・カタカナ・漢字・英数字・長音符（ー）・中点（・） |
| **絵文字** | 不可 |
| **改行・制御文字** | 不可 |
| **URL・宣伝** | 不可 |
| **NG ワード** | 公序良俗違反など（リスト運用） |
| **運営詐称** | `運営` / `ADMIN` / `GM` / `SYSTEM` / `公式` など不可 |
| **前後スペース** | 保存時に自動削除 |
| **名前の重複** | **許可**（内部 ID で個体を識別。表示名の重複は可） |
| **名前変更** | 初回は自由。以降は 1 日 1 回程度などの緩い制限 |

**将来枠（β では必須にしない）:**

- **表示名** … 画面に出す名前
- **読み** … 実況・読み上げ用（β は自動生成や省略でよい）

---



## 🚀 開発計画



### 💻 推奨サーバー構成

```
プレイヤー
   ↓
ゲームサーバー（Dedicated Game Server）
   ↓
レース状態管理（RaceManager）
```



#### ✅ メリット

- 公平な判定
- ホスト不要
- チート対策しやすい



#### 役割分担（負荷の前提）


| 処理      | 担当            |
| ------- | ------------- |
| 3D 描画   | クライアント（Godot） |
| AI 思考   | クライアント側（初期方針） |
| 位置同期・判定 | サーバー          |


サーバーは「位置同期・判定」に寄せる。描画や重めの AI をサーバーに載せないことで、小規模 VPS でも複数レースを回しやすくする。

#### 初期ホスティング想定（さくら VPS 2GB）


| 項目     | 値                                         |
| ------ | ----------------------------------------- |
| プラン例   | さくら VPS（仮想 3 Core / メモリ 2GB / SSD 100GB）  |
| 用途イメージ | 一般 Web・WordPress 向け枠でも、本ゲームの同期サーバーとしては現実的 |
| 参考料金感  | 月額換算おおよそ 1,600 円前後（プラン改定があり得るため公式確認）      |


詳細な同時開催数・通信量・拡張方針は、後述の「サーバー構成案」を参照。

### 🛠️ 開発技術スタック


| 役割         | 推奨ツール                       | 備考                       |
| ---------- | --------------------------- | ------------------------ |
| **クライアント** | Godot **4.7**（stable） | カスタマイズ性の高いオープンソースゲームエンジン。API は 4.7 公式に合わせて実装 |
| **サーバー**   | Python (asyncio)            | UDP / WebSocket を使用      |
| **データ管理**  | SQLite（初期）→ 必要なら PostgreSQL | 移行可能な設計にしておく             |


### 📁 リポジトリ構成

モノレポ。クライアントとサーバーを同じリポジトリで開発する。

```
pururin-racers/
├── client/          # Godot（project.godot はここ）
├── server/          # Python asyncio
├── shared/          # 通信契約・両側で揃える定義（中身は必要に応じて追加）
├── build/           # エクスポート成果物（Git 除外）
├── tools/godot/     # エディタ本体のローカル置き場（Git 除外・Godot 4.7）
├── docs/            # 設計案・検討事項
├── .cursor/         # 開発用エージェント設定（公開時は外す想定）
└── README.md
```

| パス | 役割 |
|------|------|
| `client/` | プレイヤー向け Godot アプリ。Win/Linux（Steam）・Android（Play）は **同一プロジェクトの Export** |
| `server/` | 位置同期・判定など Dedicated Game Server |
| `shared/` | プロトコル定義など最小の共有。ゲーム実装本体は置かない |
| `build/` | Export 成果物（`linux/` / `windows/` / `android/` など）。ソースと混ぜない・Git に上げない |
| `tools/godot/` | Godot 4.7 エディタのローカル配置（任意・Git 除外） |
| `docs/` | 仕様の正本・検討メモ |

※ PC 用／Android 用にクライアントツリーを分けない。

### 🧭 開発の進め方

クライアントとサーバーを **並行マイルストーン（薄並行）** で進める（検討事項 #20）。各段の完了は Client 出口と Server 出口の両方。オフラインは導入習熟まで（習熟・回線）。本番相当判定はサーバー権威に寄せ、クライアントに本番シミュを厚くしない。`shared/` は初回ワイヤから薄い契約を置く。

#### マイルストーン表（仮）

実装しながら段の前後入れ替え・統合してよい。正本の議論経緯は検討事項 #19。

| 段 | Client | Server | shared | 触れる体験出口 |
|----|--------|--------|--------|----------------|
| **M0** | 空シーン起動 | プロセス起動 | 空で可 | 両側が手元で動く |
| **M1** | Path＋1体走行 | ping／health | 契約開始 | 走った＋pong |
| **M2** | カーブ／内外のたたき | 空部屋 join | 部屋・エラー | 内外に差／控室に入れる |
| **M3** | 導入骨格（断片可）＋実接続UI | 接続維持（権威なし） | 接続ライフサイクル | オフライン入口＋繋がる |
| **M4** | 3体前後ローカル集団（仮判定可） | 厚くしない | 増やさない | プロト感覚（内外・集団に意味） |
| **M5** | 描画・入力送信寄せ | 権威の薄い1レース | tick／結果 | 本番気配の1本 |
| **M6+** | 導入磨き・同梱 | 非ランク＝同権威経路 | 必要なら拡張 | 練習も本番経路 |

- **進捗（2026/08/26）:** **M0〜M2 出口達成**（たたき）。Client＝競馬場形・内外・距離倍率（幾何＋BLEND）。Server／shared＝控室 `join_room`（[`shared/protocol_m2.md`](../shared/protocol_m2.md)）。次は M3
- **初期プロト完成の実務目安:** M4（下記「初期プロトタイプの完成条件」）。M5 にずらさない
- **M5 より前に本実装しない:** 本格育成UI、属性変更、開催サイクル、ランク、8頭完成、client 本番シミュの厚積み

### 📦 コース・競技場アセットの配布

**ハイブリッド**（最初からこの方針で進める）。

| データ | 配布 |
|--------|------|
| チュート／導入用の最小コース | アプリ同梱 |
| 基本コース一式（初期は少数） | アプリ同梱 |
| 重い追加（大型見た目・追加コース・季節版など） | オンデマンド DL 後、**端末に保存**して再利用 |

- レース確定のたびにフル DL しない
- 更新時のみ再取得
- 幾何（Path 等）より、競技場の見た目アセットの方が肥大化しやすい想定

### 🎯 初期プロトタイプの完成条件

以下が成立していること。

- ✅ 内側を選ぶ理由がある
- ✅ 外側を選ぶ理由がある
- ✅ 集団に入る理由がある
- ✅ 常に最適な位置が存在しない
- ✅ コースによって有利戦術が変化する

**実装上の前提:**

- 複雑な物理計算を避ける
- Path3D でコース作成
- distance + offset 方式で移動
- 距離倍率による内外差
- 初期のカーブ負荷式（`speed × curve_strength`）

**最初の目標イメージ:**

> 「3 体程度の生物が走り、内側・外側・集団のいずれにも意味がある」状態を作る

---



## 🏁 レースシステム（ベータ版向け）



### 📅 初期開催スケジュール

サーバー起動後、**10 分ごとに自動でレースを開催**。距離は以下の順序でループします。

```
スプリント (1200m) → マイル (1600m) → 2000m → 2400m → 3000m → スプリントへ戻る...
```



#### ⏱️ 開催サイクルの構成（10 分間）


| フェーズ | 時間 | 内容 |
| ------ | ---- | ---- |
| **受付・確定** | 0:00-2:00（2 分） | プレイヤーが登録・準備。受付締切時点で空き枠を **CPU（NPC）で補充し、常に 8 頭** で確定する |
| **出走確認・作戦** | 2:00-3:00（1 分） | 出走ぷるりん情報の確認、作戦タイム（脚質・指示の最終確認など） |
| **レース** | 3:00-8:00（最大 5 分） | 実際のレース進行。距離によって短くてよい。**上限は 5 分** |
| **結果・次へ** | 8:00-10:00（2 分） | 順位発表、表彰、次のレース案内 |


※ サイクル全体は **10 分固定**。レース本体を 9 分取る旧案は、長すぎるため採用しない。


---



### 📏 距離カテゴリー定義


| カテゴリー          | 距離            | 特徴                   |
| -------------- | ------------- | -------------------- |
| **短距離（スプリント）** | 1200m         | 加速・最高速が重要。直線勝負       |
| **中距離**        | 1600m, 2000m  | スタミナとラストスパートのバランスが必要 |
| **長距離**        | 2400m, 3000m  | スタミナ・集団適性が重要。持久戦     |

※ 距離感は実際の競馬を参考にする。中距離の 1600m は開催スケジュール上「マイル」と呼ぶ。


---



### 🛣️ コースタイプ実装（3 つのコース）



#### 1. **広い場所型（東京競馬場風）**

- **特徴**: 直線が長く、緩やかなカーブが多い
- **得意な生物**: 最高速・加速が高いもの
- **戦略**: ドラフティングで集団を維持しつつ、終盤にスパート



#### 2. **狭い所型（地方競馬場風）**

- **特徴**: 小回りのきいた急カーブが多い（絶対幅は推奨コース幅内）
- **得意な生物**: 操作性・旋回性能が高いもの
- **戦略**: 内側ラインを巧みに使い、外側から追い抜く



#### 3. **円形オーバル型**

- **特徴**: 完全な円形で、内外の距離差が明確
- **得意な生物**: コーナリング性能・スタミナが高いもの
- **戦略**: 内側を維持しつつ、外側からの追い抜きを狙う



#### 🔄 コースタイプの拡張性

初期は上記 3 つのみですが、**後から追加可能な設計**にしています。

```yaml
コースタイプ:
  - name: "wide_field"      # 広い場所型
    characteristics: { straight_ratio: 0.6, curve_sharpness: 0.4 }

  - name: "tight_track"     # 狭い所型
    characteristics: { straight_ratio: 0.3, curve_sharpness: 0.8 }

  - name: "oval"            # 円形オーバル型
    characteristics: { straight_ratio: 0.2, curve_sharpness: 1.0 }
```

---



### 🖥️ サーバー構成案（1 サーバーでの複数レース同時開催）



#### アーキテクチャ概要

```
┌─────────────┐     ┌──────────────────┐     ┌─────────────┐
│   プレイヤー │ UDP │  ゲームサーバー   │ WebSocket │ データベース
│    (Godot)  │ ←→  │  (Python asyncio) │ ←→    │  (SQLite)  │
└─────────────┘     └────────┬─────────┘     └─────────────┘
                             │
                    ┌────────▼────────┐
                    │ GameServer      │
                    │ ├ LoginManager  │
                    │ ├ MatchMaking   │
                    │ └ RaceManager   │
                    │    ├ Race#1001  │
                    │    ├ Race#1002  │
                    │    └ Race#1003  │
                    └─────────────────┘
```



#### 🔑 設計原則：1 レース = 1 管理単位

全レースを 1 つの巨大ループで回すと、重いレースが他レースまで遅延させる。  
そのため **Race ごとに独立**させ、asyncio では **1 レース = 1 Task** とする。

```
GameServer
│
├ RaceManager
│   ├ Race#1001  → asyncio Task（race_loop）
│   ├ Race#1002  → asyncio Task（race_loop）
│   └ Race#1003  → asyncio Task（race_loop）
│
├ MatchMaking
├ LoginManager
└ Database
```

**データ構造のイメージ:**

```python
class Race:
    race_id: int
    players: list
    state: str
    def tick(self):
        ...

class RaceManager:
    races: dict[int, Race]  # {1001: Race(), 1002: Race(), ...}
```

- プレイヤーは `player.race_id` だけ持つ
- 受信パケットは `races[player.race_id]` へ流すだけ
- 各レースはおおよそ 50ms（20Hz）ごとに独立して `tick()` する

**メリット:**


| 観点       | 効果                        |
| -------- | ------------------------- |
| **負荷分離** | 満員レースが重くても、他レースへの影響を抑えやすい |
| **障害隔離** | 1 レースのバグで全サーバー落ちになりにくい    |
| **将来拡張** | レース単位で別 VPS へシャーディングしやすい  |


**β では不要だが最初から入れておくこと:**

- `Race.tick()` を独立させる
- `RaceManager` 経由でのみレースを管理する

β は「1 VPS + RaceManager + 同時 5〜10 レース」で十分。構造だけ先に分離しておくと、後から 1 台 → 2 台 → 10 台へ拡張しやすい。

#### 📈 将来の拡張（シャーディング）

人気が出て同時レース数が増えた場合の分割イメージ:

```
ServerA: Race1〜50
ServerB: Race51〜100
```

さらに進めると:

```
Login Server
    ↓
Lobby Server
    ↓
Race Server 1 / Race Server 2 / Race Server 3
```

ロビーが空きのある Race Server へプレイヤーを振り分ける。

#### 📊 通信量の概算（20Hz）

前提: 1 頭あたり送信情報は `id / distance / offset / speed / 状態フラグ` 程度。圧縮後おおよそ 32 byte。

| 項目 | 8 頭（基本） | 12 頭（上限） |
|------|-------------|--------------|
| 1 回の送信 | 約 256 byte | 約 384 byte |
| 1 クライアント向け | 約 5 KB/s | 約 7.5 KB/s |
| 全員へ配信（1 レース） | 約 40 KB/s | 約 90 KB/s |

帯域（例: 100Mbps 共有）はボトルネックになりにくい。問題になりやすいのは **CPU**（ドラフティング判定・簡易衝突・順位計算など）。基本 8 頭の方がサーバー負荷は軽い。

#### 🧮 さくら VPS 2GB（3vCPU）での同時開催目安

| 条件 | 同時レース目安 | 人数目安（基本 8 頭） | 人数目安（12 頭時） |
|------|----------------|---------------------|-------------------|
| **軽い実装**（物理なし・サーバー AI なし・当たり判定簡易） | 20〜50 レースの可能性 | — | — |
| **現実的な安全圏**（Python asyncio + SQLite + ログ + Web 管理） | **5〜10 レース** | **約 40〜80 人** | **約 60〜120 人** |
| **Phase1〜β の運用目標** | **同時 5 レース前後** | **約 40 人程度** | — |
| **正式版の目標レンジ** | **同時 10〜20 レース** | **約 80〜160 人程度** | **約 120〜240 人程度** |


**スケール方針:**

- 開発初期〜β: さくら VPS **2GB で十分**（最初は余る想定）
- 同時 100 人を超えてきたら **4GB へスケールアップ**を検討

**2GB が厳しくなりやすい要因（サーバー側に載せ始めた場合）:**

- サーバー側 AI / NPC 思考
- 本格的な物理判定・集団押し合い
- リプレイ保存 / 毎 tick 保存
- Discord 連携・重いレーティング計算など



#### 📊 レース管理設計（運用パラメータ）

- **同時開催数**: β は **5〜10 レース**を安全圏とする（初期検証は 2〜4 からでも可）
- **各レースの独立性**: 位置データは完全に分離、干渉なし（1 レース = 1 Task）
- **通信負荷最適化**:
  - 1 レースあたり 20〜30ms 間隔（おおよそ 20Hz 前後）で位置更新
  - プレイヤーは参加しているレースのみを受信



#### 🎯 結果計算ロジック

```python
# 簡易的な結果計算のフロー（風要因なし）
def calculate_race_result(race_data):
    # 1. 各生物の最終距離を計算
    final_distances = []
    for participant in race_data.participants:
        distance = (
            base_distance * speed_factor(participant) +
            stamina_bonus(participant, race_data.distance_category) +
            drafting_bonus(participant, race_data.position_history) +
            course_bonus(participant, race_data.course_type)
        )
        final_distances.append(distance)

    # 2. ラストスパート判定（最終 10% の距離）
    sprint_phase = final_distance * 0.9
    for participant in race_data.participants:
        if participant.distance_at_sprint < participant.final_distance - threshold:
            participant.award("sprint_bonus")

    # 3. 順位決定
    sorted_participants = sorted(final_distances, reverse=True)
```

---



### 🎬 リプレイシステム

`distance + offset` 方式のため、リプレイとの相性が良い。再生時は記録した値をそのまま適用すれば、同じレースを再現できる。

#### 採用方針


| 方式                | 内容                     | 方針                                   |
| ----------------- | ---------------------- | ------------------------------------ |
| **方法1: 状態記録（基本）** | 一定間隔で各生物の状態を保存し、再生時に適用 | **採用**                               |
| **方法2: 入力のみ保存**   | 操作入力だけ保存し、再シミュレーション    | **非推奨**（ドラフティング・乱数・AI が増えると完全再現が難しい） |
| **方法3: イベント＋補間**  | 例: 1 秒ごとに完全状態、その間は補間   | 容量削減オプションとして将来検討可                    |




#### 記録内容（方法1）

おおよそ 20Hz（50ms ごと）で保存する例:

```
時刻 0.00 秒
  Player1: distance=0.0,  offset=0.2,  speed=12.1
  Player2: distance=0.0,  offset=-1.1, speed=11.8

時刻 0.05 秒
  Player1: distance=0.6,  offset=0.3,  speed=12.0
  Player2: distance=0.58, offset=-1.0, speed=11.7
```

最低限でも `distance` / `offset` があればかなり再現できる。必要に応じて以下を追加する。

- speed / stamina
- animation_state / スキル発動
- 順位などメタ情報

**フレーム構造イメージ:**

```python
ReplayFrame:
    timestamp
    players:
        id
        distance
        offset
        speed
        stamina  # 任意
```



#### データ量の概算（12 頭・20Hz）


| 項目                                          | 概算       |
| ------------------------------------------- | -------- |
| 1 頭あたり（distance / offset / speed 各 float32） | 12 byte  |
| 1 フレーム（12 頭）                                | 144 byte |
| 1 秒                                         | 約 2.9 KB |
| 5 分レース（300 秒・上限）                            | 約 0.9 MB |


スキル・順位・スタミナなどを足しても、**1 レース数 MB 程度**を想定。

#### 保存形式例

```json
{
  "version": 1,
  "course": "oval01",
  "frames": [
    {
      "time": 0.0,
      "players": [
        { "id": 1, "distance": 0.0, "offset": 0.1 }
      ]
    }
  ]
}
```



#### 保存・保持方針（現時点）

- **全レースのリプレイデータを保存する**
- **保持期間は 14 日**。期限切れは自動削除する
- 大会・ランキング入りなどの「永久保存」例外は、現時点では設けない（必要になったら別途検討）

SSD 容量は 1 レース数 MB 規模なら、14 日ローテーションでも現実的な範囲に収まりやすい。

#### 再生・演出（将来）

記録データを適用するだけで再生できるため、カメラや演出にも転用しやすい。

- **カメラ**: プレイ中と同じ切替（後方デフォルト／前・横・頭上やや後方、他ぷるりんへの付け替え）。M2 たたきでは後方＋真上正射影。詳細は「カメラ・視点・ミニマップ」を参照
- ゴール前スロー
- ベストレースの再視聴

※ **観戦モード**とリプレイは内部的に近い仕組みになる想定。観戦を入れるならリプレイ基盤がほぼ必須。  
※ 観戦・リプレイでも、プレイ中と同様にアングル／対象／ミニマップ操作をいつでも使える。

---



## 📜 履歴

- 2026/07/12: 仕様設計書作成
- 2026/07/14: 仕様改善反映（最適解を作らない設計思想、距離倍率方式の内外補正、offset 単位・コース幅、カーブ率式、初期カーブ負荷、風システム廃止、開発優先順位の再定義）
- 2026/07/14: サーバー計画追記（さくら VPS 2GB の同時開催目安、通信量概算、RaceManager＝1レース1Task、将来シャーディング）
- 2026/07/14: リプレイシステム追記（状態記録方式、全レース保存・保持 14 日、観戦との関係）
- 2026/07/16: トレーナー／NPC AI 追記（3 層 AI、脚質、指示制、運要素の非採用を明確化）
- 2026/07/26: ステータス・レースシステム改訂（基本ステータス 11 種、レース中の状態値、心拍・ブースト・モチベーションシステム、属性ボーナス制、脚質ボーナス、操作方式＝左右移動・ブースト・作戦指示の明確化）
- 2026/07/30: 接触・柵・コース形状判定を追記（キャラ同士は自前距離判定、柵は offset クランプ＝接触で Collision なし、見た目柵は Path 自動生成、カーブ／坂は distance から Curve 計算）
- 2026/08/01: 見た目カスタム（3D 共通＋画像パーツ、模様は能力非影響）と命名ルール（2〜12 文字、重複 OK、表示名＋読みは将来枠）を追記
- 2026/08/01: 出走数方針を追記（基本 8 頭・エンジン最大 12 頭、コース幅とゲートは最初から 12 頭分、β は 8 頭固定）
- 2026/08/05: カメラ・視点・ミニマップ方針を追記（後方デフォルト、前／横／頭上やや後方、プレイ・リプレイ・観戦で共通切替、他個体カメラ、ミニマップ倍率・前後スクロール、前後スライド選択、カーブ時振りは保留）
- 2026/08/05: 距離カテゴリーを競馬寄りに改定（短距離 1200m、中距離 1600m/2000m、長距離 2400m/3000m は維持。開催ループのマイルも 1600m に合わせる）
- 2026/08/05: 開催サイクルを改定（10 分固定のまま、受付・確定 2 分／出走確認・作戦 1 分／レース最大 5 分／結果・次へ 2 分。締切時 CPU 補充で常に 8 頭）
- 2026/08/07: 所持スロット最大 5・枠解放なし、レシピ（配分＋脚質＋属性）を所持と分離して任意保存、他者コピーは方針・βは自前のみ、を追記
- 2026/08/07: トレーナー画面表現を追記（公式人型なし、KVは宣伝、プロフィールUI、観客席の人は可、会話・ストーリーなし、チュート立ち絵は将来可）
- 2026/08/07: 目標スピード指示を採用（現在速度は直接操作しない。出力は追従負荷の導出値。粗い刻み・帯プリセットなし。積極／温存は CPU 向けに検討）
- 2026/08/07: リポジトリ構成を追記（`client/` + `server/` + `shared/` + `docs/`）。開発フェーズ表は検討事項 #19 へ移管
- 2026/08/07: クライアントエンジンを Godot 4.7 に固定（エディタは `tools/godot/` にローカル配置・Git 除外）
- 2026/08/07: エクスポート成果物の置き場を `build/` に決定（プラットフォーム別サブフォルダ・Git 除外）
- 2026/08/07: 習熟はオフライン（チュート＋導入トライアル／クエスト）、本格練習・本番はオンライン必須。長編ストーリー・会話は引き続きなし
- 2026/08/07: アセット配布をハイブリッドに決定（基本同梱、重い追加は DL 後ローカル保存、レース毎フル DL なし）
- 2026/08/07: 開発進行を薄並行に決定（client＋server 両出口のマイルストーン。shared は接続から薄い契約。本格オンライン練習は権威スパイク後）
- 2026/08/07: 並行列マイルストーン表（M0〜M5＋M6+）を追記。設計案上は（仮）。プロト完成目安は M4。属性・育成・開催の本実装は M5 後（#19）
- 2026/08/26: M2 たたき反映。内外距離倍率＝幾何比＋`GEOMETRIC_BLEND`（直線は差なし）。カメラに真上正射影俯瞰（M2）。マイルストーン進捗 M0〜M2 出口達成。控室契約は `shared/protocol_m2.md`

